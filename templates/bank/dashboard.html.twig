{% extends 'base.html.twig' %}

{% block title %}Banque MIDDO - Dashboard{% endblock %}

{% block stylesheets %}
{{ parent() }}
<style>
    .bank-dashboard {
        max-width: 1400px;
        margin: 0 auto;
        padding: 2rem;
    }

    .balance-card {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border-radius: 20px;
        padding: 2.5rem;
        color: white;
        box-shadow: 0 10px 30px rgba(102, 126, 234, 0.3);
        margin-bottom: 2rem;
        animation: fadeInUp 0.6s ease-out;
    }

    .balance-amount {
        font-size: 3.5rem;
        font-weight: 700;
        margin: 1rem 0;
        text-shadow: 0 2px 10px rgba(0,0,0,0.2);
    }

    .balance-label {
        font-size: 1.1rem;
        opacity: 0.9;
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    .quick-actions {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 1.5rem;
        margin-bottom: 2rem;
    }

    .action-btn {
        background: white;
        border: none;
        border-radius: 15px;
        padding: 1.5rem;
        text-align: center;
        text-decoration: none;
        color: #333;
        transition: all 0.3s ease;
        box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        cursor: pointer;
    }

    .action-btn:hover {
        transform: translateY(-5px);
        box-shadow: 0 8px 25px rgba(0,0,0,0.15);
        color: #667eea;
    }

    .action-btn i {
        font-size: 2rem;
        display: block;
        margin-bottom: 0.5rem;
    }

    .stats-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 1.5rem;
        margin-bottom: 2rem;
    }

    .stat-card {
        background: white;
        border-radius: 15px;
        padding: 1.5rem;
        box-shadow: 0 4px 15px rgba(0,0,0,0.08);
    }

    .stat-value {
        font-size: 2rem;
        font-weight: 700;
        color: #667eea;
        margin: 0.5rem 0;
    }

    .stat-label {
        font-size: 0.9rem;
        color: #666;
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    .history-card {
        background: white;
        border-radius: 15px;
        padding: 2rem;
        box-shadow: 0 4px 15px rgba(0,0,0,0.08);
    }

    .history-title {
        font-size: 1.5rem;
        font-weight: 600;
        margin-bottom: 1.5rem;
        color: #333;
    }

    .transaction-item {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 1rem;
        border-bottom: 1px solid #f0f0f0;
        transition: background 0.2s ease;
    }

    .transaction-item:hover {
        background: #f9f9f9;
    }

    .transaction-item:last-child {
        border-bottom: none;
    }

    .transaction-info {
        flex: 1;
    }

    .transaction-desc {
        font-weight: 500;
        color: #333;
        margin-bottom: 0.25rem;
    }

    .transaction-date {
        font-size: 0.85rem;
        color: #999;
    }

    .transaction-amount {
        font-size: 1.2rem;
        font-weight: 600;
    }

    .transaction-amount.credit {
        color: #10b981;
    }

    .transaction-amount.debit {
        color: #ef4444;
    }

    .empty-state {
        text-align: center;
        padding: 3rem 1rem;
        color: #999;
    }

    .empty-state i {
        font-size: 4rem;
        margin-bottom: 1rem;
        opacity: 0.3;
    }

    @keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @media (max-width: 768px) {
        .bank-dashboard {
            padding: 1rem;
        }

        .balance-amount {
            font-size: 2.5rem;
        }

        .quick-actions {
            grid-template-columns: repeat(2, 1fr);
        }
    }
</style>
{% endblock %}

{% block body %}
<div class="bank-dashboard">
    <!-- Card Solde Principal -->
    <div class="balance-card">
        <div class="balance-label">💰 Solde disponible</div>
        <div class="balance-amount">{{ balance_formatted }} {{ currency }}</div>
        <div class="balance-label">Compte N° {{ account.accountNumber }}</div>
    </div>

    <!-- Actions Rapides -->
    <div class="quick-actions">
        <a href="{{ path('app_bank_transfer') }}" class="action-btn">
            <i class="fas fa-exchange-alt"></i>
            <strong>Transférer</strong>
            <small class="d-block mt-1">Envoyer de l'argent</small>
        </a>
        <a href="{{ path('app_bank_deposit') }}" class="action-btn">
            <i class="fas fa-plus-circle"></i>
            <strong>Déposer</strong>
            <small class="d-block mt-1">Recharger mon compte</small>
        </a>
        <a href="{{ path('app_bank_withdraw') }}" class="action-btn">
            <i class="fas fa-minus-circle"></i>
            <strong>Retirer</strong>
            <small class="d-block mt-1">Vers mon compte bancaire</small>
        </a>
    </div>

    <!-- Statistiques 7 derniers jours -->
    <div class="stats-grid">
        <div class="stat-card">
            <div class="stat-label">📈 Reçu (7 jours)</div>
            <div class="stat-value text-success">+{{ stats.total_in_formatted }} €</div>
        </div>
        <div class="stat-card">
            <div class="stat-label">📉 Envoyé (7 jours)</div>
            <div class="stat-value text-danger">-{{ stats.total_out_formatted }} €</div>
        </div>
        <div class="stat-card">
            <div class="stat-label">🔄 Transactions</div>
            <div class="stat-value">{{ stats.transaction_count }}</div>
        </div>
    </div>

    <!-- Historique des transactions -->
    <div class="history-card">
        <h2 class="history-title">📜 Historique récent</h2>

        {% if history is empty %}
            <div class="empty-state">
                <i class="fas fa-inbox"></i>
                <p>Aucune transaction pour le moment</p>
                <small>Vos futures transactions apparaîtront ici</small>
            </div>
        {% else %}
            <div class="transaction-list">
                {% for entry in history %}
                    <div class="transaction-item">
                        <div class="transaction-info">
                            <div class="transaction-desc">
                                {{ entry.description ?? 'Transaction' }}
                            </div>
                            <div class="transaction-date">
                                {{ entry.createdAt|date('d/m/Y à H:i') }}
                            </div>
                        </div>
                        <div class="transaction-amount {{ entry.entryType|lower }}">
                            {% if entry.entryType == 'CREDIT' %}
                                +{{ (entry.amountCents / 100)|number_format(2, ',', ' ') }} €
                            {% else %}
                                -{{ (entry.amountCents / 100)|number_format(2, ',', ' ') }} €
                            {% endif %}
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% endif %}
    </div>
</div>
{% endblock %}
