{% extends 'base_app.html.twig' %}

{% block title %}Mon Compte{% endblock %}

{% block body %}
<div class="container mx-auto px-6 py-8">
    <div class="mb-6 flex justify-between items-center">
        <h1 class="text-3xl font-bold text-gray-800">Mon Compte</h1>
        {% if not edit_mode|default(false) %}
        <a href="{{ path('app_profile_edit') }}" class="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700">
            Éditer le profil
        </a>
        {% endif %}
    </div>

    <div class="bg-white rounded-lg shadow-lg overflow-hidden">
        <div class="p-6 border-b border-gray-200 bg-gradient-to-r from-blue-500 to-purple-600">
            <div class="flex items-center">
                <div class="w-20 h-20 rounded-full bg-white flex items-center justify-center text-3xl font-bold text-blue-600">
                    {{ user.email|slice(0,1)|upper }}
                </div>
                <div class="ml-6 text-white">
                    <h2 class="text-2xl font-bold">{{ user.email }}</h2>
                    <p class="text-blue-100">Membre depuis {{ user.createdAt|date('d/m/Y') }}</p>
                </div>
            </div>
        </div>

        <div class="p-6 grid grid-cols-1 lg:grid-cols-3 gap-8">
            <div class="lg:col-span-2">
                <h3 class="text-xl font-semibold mb-4">Informations personnelles</h3>
                <dl class="space-y-4">
                    <div>
                        <dt class="text-sm font-medium text-gray-500">Email</dt>
                        <dd class="mt-1 text-sm text-gray-900">{{ user.email }}</dd>
                    </div>
                    <div>
                        <dt class="text-sm font-medium text-gray-500">Rôles</dt>
                        <dd class="mt-1 text-sm text-gray-900">
                            {% for role in user.roles %}
                            <span class="inline-block px-3 py-1 rounded-full text-xs font-medium bg-blue-100 text-blue-800 mr-2">
                                {{ role }}
                            </span>
                            {% endfor %}
                        </dd>
                    </div>
                    <div>
                        <dt class="text-sm font-medium text-gray-500">Compte créé le</dt>
                        <dd class="mt-1 text-sm text-gray-900">{{ user.createdAt|date('d F Y à H:i') }}</dd>
                    </div>
                </dl>
            </div>

            <div class="space-y-6">
                <div class="bg-gray-50 p-4 rounded border border-gray-200">
                    <h3 class="font-semibold text-gray-900 mb-2">Statistiques</h3>
                    <ul class="space-y-2 text-sm">
                        <li class="flex justify-between">
                            <span class="text-gray-600">Projets</span>
                            <span class="font-semibold">{{ user.projects|length }}</span>
                        </li>
                        <li class="flex justify-between">
                            <span class="text-gray-600">Workspaces</span>
                            <span class="font-semibold">{{ user.workspaces|length }}</span>
                        </li>
                    </ul>
                </div>

                <div class="bg-blue-50 p-4 rounded border border-blue-200">
                    <h3 class="font-semibold text-blue-900 mb-2">Actions rapides</h3>
                    <ul class="space-y-2">
                        <li>
                            <a href="{{ path('app_settings') }}" class="text-blue-600 hover:underline text-sm">
                                ⚙️ Paramètres du compte
                            </a>
                        </li>
                        <li>
                            <a href="#" class="text-blue-600 hover:underline text-sm">
                                🔒 Changer le mot de passe
                            </a>
                        </li>
                        <li>
                            <a href="#" class="text-red-600 hover:underline text-sm">
                                🚪 Se déconnecter
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>

    {% if edit_mode|default(false) %}
    <div class="mt-8 bg-yellow-50 border border-yellow-200 rounded p-4">
        <p class="text-sm text-yellow-800">
            ⚠️ <strong>Mode édition :</strong> Formulaire d'édition à implémenter (TODO)
        </p>
    </div>
    {% endif %}
</div>
{% endblock %}