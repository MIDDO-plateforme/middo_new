{# ============================================================
   FICHIER : templates/components/_menu.html.twig
   Menu principal MIDDO (sidebar)
   ============================================================ #}

<ul class="sidebar-menu-list" style="
    list-style:none;
    padding:0;
    margin:0;
    display:flex;
    flex-direction:column;
    gap:0.75rem;
">

    {# Tableau de bord global #}
    <li>
        <a href="{{ path('global_dashboard') }}" class="sidebar-link" style="
            display:flex;
            align-items:center;
            gap:0.75rem;
            padding:0.6rem 0.8rem;
            border-radius:6px;
            text-decoration:none;
            color:#333;
            font-size:0.95rem;
        ">
            <span class="menu-icon">📊</span>
            <span class="menu-label">Dashboard global</span>
        </a>
    </li>

    {# Transport #}
    <li>
        <a href="{{ path('transport_dashboard') }}" class="sidebar-link" style="
            display:flex;
            align-items:center;
            gap:0.75rem;
            padding:0.6rem 0.8rem;
            border-radius:6px;
            text-decoration:none;
            color:#333;
            font-size:0.95rem;
        ">
            <span class="menu-icon">🚚</span>
            <span class="menu-label">Transport</span>
        </a>
    </li>

    {# Banque #}
    <li>
        <a href="{{ path('bank_dashboard') }}" class="sidebar-link" style="
            display:flex;
            align-items:center;
            gap:0.75rem;
            padding:0.6rem 0.8rem;
            border-radius:6px;
            text-decoration:none;
            color:#333;
            font-size:0.95rem;
        ">
            <span class="menu-icon">🏦</span>
            <span class="menu-label">Banque</span>
        </a>
    </li>

    {# Juridique #}
    <li>
        <a href="{{ path('legal_dashboard') }}" class="sidebar-link" style="
            display:flex;
            align-items:center;
            gap:0.75rem;
            padding:0.6rem 0.8rem;
            border-radius:6px;
            text-decoration:none;
            color:#333;
            font-size:0.95rem;
        ">
            <span class="menu-icon">⚖️</span>
            <span class="menu-label">Juridique</span>
        </a>
    </li>

    {# Workplace #}
    <li>
        <a href="{{ path('workplace') }}" class="sidebar-link" style="
            display:flex;
            align-items:center;
            gap:0.75rem;
            padding:0.6rem 0.8rem;
            border-radius:6px;
            text-decoration:none;
            color:#333;
            font-size:0.95rem;
        ">
            <span class="menu-icon">💼</span>
            <span class="menu-label">Espace de travail</span>
        </a>
    </li>

    {# Administration (si rôle admin) #}
    {% if 'ROLE_ADMIN' in app.user.roles %}
        <li>
            <a href="{{ path('admin_dashboard') }}" class="sidebar-link" style="
                display:flex;
                align-items:center;
                gap:0.75rem;
                padding:0.6rem 0.8rem;
                border-radius:6px;
                text-decoration:none;
                color:#333;
                font-size:0.95rem;
            ">
                <span class="menu-icon">🛠️</span>
                <span class="menu-label">Administration</span>
            </a>
        </li>
    {% endif %}

</ul>
