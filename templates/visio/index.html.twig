{% extends 'layout/app_layout.html.twig' %}

{% block page_title %}
    Visioconférences
{% endblock %}

{% block page_subtitle %}
    Gérez vos réunions, salles et comptes-rendus IA
{% endblock %}

{% block page_actions %}
    <button class="btn btn-primary">Créer une salle</button>
    <button class="btn-outline">Rejoindre</button>
    <button class="btn-outline">Analyse IA</button>
{% endblock %}

{% block page_content %}

{# --- Statistiques Visio --- #}
<div class="visio-stats-grid">

    {% include 'components/project_stat_card.html.twig' with {
        title: 'Réunions aujourd’hui',
        value: 4,
        color: 'blue'
    } %}

    {% include 'components/project_stat_card.html.twig' with {
        title: 'En cours',
        value: 1,
        color: 'green'
    } %}

    {% include 'components/project_stat_card.html.twig' with {
        title: 'Planifiées',
        value: 6,
        color: 'purple'
    } %}

    {% include 'components/project_stat_card.html.twig' with {
        title: 'Comptes-rendus IA',
        value: 3,
        color: 'orange'
    } %}
</div>

{# --- Liste des salles --- #}
<div class="visio-grid">

    {% for room in rooms %}
        <div class="visio-card">

            <h3>{{ room.title }}</h3>

            <p class="visio-desc">{{ room.description }}</p>

            <div class="visio-meta">
                <span>🕒 {{ room.time }}</span>
                <span>👥 {{ room.participants|length }} participants</span>
            </div>

            <div class="visio-participants">
                {% for p in room.participants %}
                    <span class="participant-avatar">{{ p }}</span>
                {% endfor %}
            </div>

            <div class="actions">
                <a href="{{ path('visio_index') ~ '/' ~ room.id }}" class="btn">Rejoindre</a>
                <a href="{{ path('visio_index') ~ '/' ~ room.id ~ '/summary' }}" class="btn-outline">Résumé IA</a>
            </div>

        </div>
    {% endfor %}

</div>

{% endblock %}
