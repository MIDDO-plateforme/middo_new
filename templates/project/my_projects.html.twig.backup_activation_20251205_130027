{% extends 'base.html.twig' %}

{% block title %}Mes Projets{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/ai-styles.css') }}">
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/ai-features.js') }}"></script>
{% endblock %}

{% block body %}
<div class="container mx-auto px-4 py-8">
    <h1 class="text-3xl font-bold mb-6">Mes Projets</h1>
    <p class="text-gray-600 mb-8">Gérez vos projets d'entreprise</p>

    <!-- BOUTONS IA MIDDO -->
    <div class="ai-features-buttons" style="margin: 20px 0; display: flex; gap: 10px; flex-wrap: wrap;">
        <button class="btn btn-ai" data-ai="chatbot" data-project-id="{{ app.user ? app.user.id : '' }}" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; border: none; padding: 10px 20px; border-radius: 8px; cursor: pointer; font-weight: bold;" data-action="sentiment" data-project-id="{{ app.user ? app.user.id : '' }}">
            🤖 Assistant IA
        </button>
        <button class="btn btn-ai" data-ai="suggestions" data-project-id="{{ app.user ? app.user.id : '' }}" style="background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%); color: white; border: none; padding: 10px 20px; border-radius: 8px; cursor: pointer; font-weight: bold;" data-action="suggestions" data-project-id="{{ app.user ? app.user.id : '' }}" data-action="sentiment" data-project-id="{{ app.user ? app.user.id : '' }}">
            ✨ Améliorer
        </button>
        <button class="btn btn-ai" data-ai="matching" data-project-id="{{ app.user ? app.user.id : '' }}" style="background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%); color: white; border: none; padding: 10px 20px; border-radius: 8px; cursor: pointer; font-weight: bold;" data-action="matching" data-project-id="{{ app.user ? app.user.id : '' }}" data-action="sentiment" data-project-id="{{ app.user ? app.user.id : '' }}">
            👥 Trouver des Profils
        </button>
        <button class="btn btn-ai" data-ai="sentiment" data-project-id="{{ app.user ? app.user.id : '' }}" style="background: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%); color: white; border: none; padding: 10px 20px; border-radius: 8px; cursor: pointer; font-weight: bold;" data-action="sentiment" data-project-id="{{ app.user ? app.user.id : '' }}">
            😊 Analyser le Ton
        </button>
    </div>

    <!-- BOUTON CRÉER PROJET -->
    <a href="{{ path('app_project_new') }}" class="btn btn-primary inline-block mb-6 bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded">
        Créer un projet
    </a>

    <!-- LISTE DES PROJETS -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {% for project in projects %}
        <div class="bg-white rounded-lg shadow-md p-6 hover:shadow-lg transition">
            <h3 class="text-xl font-bold mb-2">{{ project.title }}</h3>
            <p class="text-gray-600 mb-4">{{ project.description|slice(0, 100) }}...</p>
            
            {% if project.budget %}
            <p class="text-sm text-gray-500 mb-2">
                <strong>Budget :</strong> {{ project.budget }} FCFA
            </p>
            {% endif %}
            
            <p class="text-sm text-gray-500 mb-4">
                <strong>Créé le</strong> {{ project.createdAt|date('d/m/Y') }}
            </p>
            
            <div class="flex gap-2">
                <a href="{{ path('app_project_show', {'id': project.id}) }}" class="btn btn-sm bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded">
                    Voir
                </a>
                <a href="{{ path('app_project_edit', {'id': project.id}) }}" class="btn btn-sm bg-gray-500 hover:bg-gray-600 text-white px-4 py-2 rounded">
                    Modifier
                </a>
            </div>
        </div>
        {% else %}
        <div class="col-span-full text-center py-12">
            <p class="text-gray-500 text-lg">Vous n'avez pas encore de projets.</p>
            <a href="{{ path('app_project_new') }}" class="btn btn-primary mt-4 inline-block bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded">
                Créer votre premier projet
            </a>
        </div>
        {% endfor %}
    </div>
</div>

<!-- MODALES IA (gérées par ai-features.js) -->
<div id="ai-modal" class="ai-modal" style="display: none;">
    <div class="ai-modal-content">
        <div class="ai-modal-header">
            <h2 id="modal-title">Assistant IA</h2>
            <button class="ai-modal-close" onclick="document.getElementById('ai-modal').style.display='none'">&times;</button>
        </div>
        <div class="ai-modal-body" id="modal-body">
            <!-- Contenu dynamique injecté par JavaScript -->
        </div>
    </div>
</div>

<script src="{{ asset('js/diagnostic-boutons.js') }}"></script>
{% endblock %}
