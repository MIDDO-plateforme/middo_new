{% extends 'base.html.twig' %}

{% block title %}MIDDO Dashboard - Analytics{% endblock %}

{% block body %}
<style>
    /* ===================================
       VARIABLES & BASE STYLES
    =================================== */
    :root {
        --primary: #6366f1;
        --success: #10b981;
        --warning: #f59e0b;
        --danger: #ef4444;
        --info: #3b82f6;
        --dark: #1f2937;
        --light: #f9fafb;
        --border: #e5e7eb;
    }

    .dashboard-container {
        padding: 2rem;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
    }

    /* ===================================
       HEADER SECTION
    =================================== */
    .dashboard-header {
        background: white;
        border-radius: 16px;
        padding: 2rem;
        margin-bottom: 2rem;
        box-shadow: 0 4px 6px rgba(0,0,0,0.1);
    }

    .dashboard-header h1 {
        font-size: 2rem;
        font-weight: 700;
        color: var(--dark);
        margin: 0 0 0.5rem 0;
    }

    .session-badge {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        background: linear-gradient(135deg, var(--primary), var(--info));
        color: white;
        padding: 0.5rem 1.5rem;
        border-radius: 24px;
        font-weight: 600;
        font-size: 0.875rem;
        animation: pulse 2s infinite;
    }

    .status-badge {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        background: var(--success);
        color: white;
        padding: 0.5rem 1rem;
        border-radius: 24px;
        font-weight: 600;
        font-size: 0.875rem;
        margin-left: 1rem;
    }

    .status-dot {
        width: 8px;
        height: 8px;
        background: white;
        border-radius: 50%;
        animation: blink 1.5s infinite;
    }

    @keyframes pulse {
        0%, 100% { transform: scale(1); }
        50% { transform: scale(1.05); }
    }

    @keyframes blink {
        0%, 100% { opacity: 1; }
        50% { opacity: 0.3; }
    }

    /* ===================================
       KPI CARDS
    =================================== */
    .kpi-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 1.5rem;
        margin-bottom: 2rem;
    }

    .kpi-card {
        background: white;
        border-radius: 16px;
        padding: 1.5rem;
        box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        transition: all 0.3s ease;
        position: relative;
        overflow: hidden;
    }

    .kpi-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 12px 24px rgba(0,0,0,0.15);
    }

    .kpi-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 4px;
        background: linear-gradient(90deg, var(--card-color), transparent);
    }

    .kpi-card.users { --card-color: var(--info); }
    .kpi-card.notifications { --card-color: var(--warning); }
    .kpi-card.tasks { --card-color: var(--success); }
    .kpi-card.engagement { --card-color: var(--primary); }

    .kpi-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1rem;
    }

    .kpi-icon {
        width: 48px;
        height: 48px;
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 24px;
        background: linear-gradient(135deg, var(--card-color), transparent);
        color: white;
    }

    .kpi-title {
        font-size: 0.875rem;
        color: #6b7280;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.05em;
    }

    .kpi-value {
        font-size: 2.5rem;
        font-weight: 700;
        color: var(--dark);
        line-height: 1;
        margin-bottom: 0.5rem;
    }

    .kpi-subtitle {
        font-size: 0.875rem;
        color: #9ca3af;
        margin-bottom: 1rem;
    }

    .kpi-trend {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        font-size: 0.875rem;
        font-weight: 600;
    }

    .kpi-trend.positive {
        color: var(--success);
    }

    .kpi-trend.negative {
        color: var(--danger);
    }

    .sparkline {
        width: 100%;
        height: 40px;
        margin-top: 1rem;
        background: linear-gradient(to right, rgba(99, 102, 241, 0.1), rgba(99, 102, 241, 0.3));
        border-radius: 8px;
        position: relative;
        overflow: hidden;
    }

    .sparkline::after {
        content: '';
        position: absolute;
        bottom: 0;
        left: 0;
        width: 100%;
        height: 60%;
        background: linear-gradient(to right, 
            transparent 0%, var(--card-color) 25%, 
            var(--card-color) 50%, transparent 75%, 
            var(--card-color) 100%);
        opacity: 0.3;
        clip-path: polygon(
            0% 100%, 10% 80%, 20% 85%, 30% 60%, 
            40% 70%, 50% 40%, 60% 55%, 70% 30%, 
            80% 45%, 90% 20%, 100% 35%, 100% 100%
        );
    }

    /* ===================================
       CHARTS GRID
    =================================== */
    .charts-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(500px, 1fr));
        gap: 1.5rem;
        margin-bottom: 2rem;
    }

    .chart-card {
        background: white;
        border-radius: 16px;
        padding: 1.5rem;
        box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        transition: all 0.3s ease;
    }

    .chart-card:hover {
        box-shadow: 0 12px 24px rgba(0,0,0,0.15);
    }

    .chart-header {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        margin-bottom: 1.5rem;
        padding-bottom: 1rem;
        border-bottom: 2px solid var(--border);
    }

    .chart-icon {
        font-size: 24px;
    }

    .chart-title {
        font-size: 1.25rem;
        font-weight: 700;
        color: var(--dark);
        margin: 0;
    }

    .chart-image {
        width: 100%;
        height: auto;
        border-radius: 12px;
        box-shadow: 0 2px 8px rgba(0,0,0,0.08);
    }

    /* ===================================
       ACTIVITY FEED
    =================================== */
    .activity-section {
        background: white;
        border-radius: 16px;
        padding: 2rem;
        box-shadow: 0 4px 6px rgba(0,0,0,0.1);
    }

    .activity-header {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        margin-bottom: 1.5rem;
        padding-bottom: 1rem;
        border-bottom: 2px solid var(--border);
    }

    .activity-header h3 {
        font-size: 1.5rem;
        font-weight: 700;
        color: var(--dark);
        margin: 0;
    }

    .activity-list {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    .activity-item {
        display: flex;
        gap: 1rem;
        padding: 1rem;
        border-radius: 12px;
        background: var(--light);
        transition: all 0.3s ease;
    }

    .activity-item:hover {
        background: #f3f4f6;
        transform: translateX(5px);
    }

    .activity-avatar {
        width: 48px;
        height: 48px;
        border-radius: 50%;
        background: linear-gradient(135deg, var(--primary), var(--info));
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-size: 20px;
        flex-shrink: 0;
    }

    .activity-content {
        flex: 1;
    }

    .activity-title {
        font-weight: 600;
        color: var(--dark);
        margin-bottom: 0.25rem;
    }

    .activity-description {
        font-size: 0.875rem;
        color: #6b7280;
        margin-bottom: 0.5rem;
    }

    .activity-time {
        font-size: 0.75rem;
        color: #9ca3af;
        display: flex;
        align-items: center;
        gap: 0.25rem;
    }

    .empty-state {
        text-align: center;
        padding: 3rem 1rem;
        color: #9ca3af;
    }

    .empty-state i {
        font-size: 48px;
        margin-bottom: 1rem;
        opacity: 0.5;
    }

    /* ===================================
       RESPONSIVE
    =================================== */
    @media (max-width: 768px) {
        .dashboard-container {
            padding: 1rem;
        }

        .charts-grid {
            grid-template-columns: 1fr;
        }

        .kpi-grid {
            grid-template-columns: 1fr;
        }

        .dashboard-header h1 {
            font-size: 1.5rem;
        }

        .kpi-value {
            font-size: 2rem;
        }
    }
</style>

<div class="dashboard-container">
    <!-- HEADER -->
    <div class="dashboard-header">
        <h1>📊 MIDDO Dashboard</h1>
        <div>
            <span class="session-badge">
                <i class="bi bi-graph-up"></i>
                SESSION 24 - Analytics en temps réel
            </span>
            <span class="status-badge">
                <span class="status-dot"></span>
                Opérationnel
            </span>
        </div>
    </div>

    <!-- KPI CARDS -->
    <div class="kpi-grid">
        <!-- CARD 1: USERS -->
        <div class="kpi-card users">
            <div class="kpi-header">
                <div class="kpi-icon">
                    <i class="bi bi-people-fill"></i>
                </div>
            </div>
            <div class="kpi-title">Total Utilisateurs</div>
            <div class="kpi-value">5</div>
            <div class="kpi-subtitle">Membres actifs</div>
            <div class="kpi-trend positive">
                <i class="bi bi-arrow-up-circle-fill"></i>
                +12% cette semaine
            </div>
            <div class="sparkline"></div>
        </div>

        <!-- CARD 2: NOTIFICATIONS -->
        <div class="kpi-card notifications">
            <div class="kpi-header">
                <div class="kpi-icon">
                    <i class="bi bi-bell-fill"></i>
                </div>
            </div>
            <div class="kpi-title">Notifications</div>
            <div class="kpi-value">1</div>
            <div class="kpi-subtitle">1 non lues</div>
            <div class="kpi-trend negative">
                <i class="bi bi-arrow-down-circle-fill"></i>
                -5% depuis hier
            </div>
            <div class="sparkline"></div>
        </div>

        <!-- CARD 3: TASKS -->
        <div class="kpi-card tasks">
            <div class="kpi-header">
                <div class="kpi-icon">
                    <i class="bi bi-check-circle-fill"></i>
                </div>
            </div>
            <div class="kpi-title">Tâches</div>
            <div class="kpi-value">5</div>
            <div class="kpi-subtitle">40% terminées</div>
            <div class="kpi-trend positive">
                <i class="bi bi-arrow-up-circle-fill"></i>
                +8% aujourd'hui
            </div>
            <div class="sparkline"></div>
        </div>

        <!-- CARD 4: ENGAGEMENT -->
        <div class="kpi-card engagement">
            <div class="kpi-header">
                <div class="kpi-icon">
                    <i class="bi bi-bar-chart-fill"></i>
                </div>
            </div>
            <div class="kpi-title">Engagement</div>
            <div class="kpi-value">0%</div>
            <div class="kpi-subtitle">Taux de lecture</div>
            <div class="kpi-trend negative">
                <i class="bi bi-arrow-down-circle-fill"></i>
                En attente de données
            </div>
            <div class="sparkline"></div>
        </div>
    </div>

    <!-- CHARTS GRID (4 GRAPHIQUES PNG) -->
    <div class="charts-grid">
        <!-- CHART 1: COMPLETION RATE -->
        <div class="chart-card">
            <div class="chart-header">
                <span class="chart-icon">📈</span>
                <h3 class="chart-title">Évolution du Taux de Complétion (%)</h3>
            </div>
            <img src="{{ asset('images/charts/chart_completion.png') }}" 
                 alt="Évolution du Taux de Complétion" 
                 class="chart-image">
        </div>

        <!-- CHART 2: PRODUCTIVITY -->
        <div class="chart-card">
            <div class="chart-header">
                <span class="chart-icon">📊</span>
                <h3 class="chart-title">Score de Productivité Quotidien</h3>
            </div>
            <img src="{{ asset('images/charts/chart_productivity.png') }}" 
                 alt="Score de Productivité" 
                 class="chart-image">
        </div>

        <!-- CHART 3: TASKS DISTRIBUTION -->
        <div class="chart-card">
            <div class="chart-header">
                <span class="chart-icon">🥧</span>
                <h3 class="chart-title">Répartition des Tâches</h3>
            </div>
            <img src="{{ asset('images/charts/chart_tasks.png') }}" 
                 alt="Répartition des Tâches" 
                 class="chart-image">
        </div>

        <!-- CHART 4: NOTIFICATIONS -->
        <div class="chart-card">
            <div class="chart-header">
                <span class="chart-icon">🔔</span>
                <h3 class="chart-title">Notifications : Lues vs Non Lues</h3>
            </div>
            <img src="{{ asset('images/charts/chart_notifications.png') }}" 
                 alt="Notifications Lues vs Non Lues" 
                 class="chart-image">
        </div>
    </div>

    <!-- ACTIVITY FEED -->
    <div class="activity-section">
        <div class="activity-header">
            <i class="bi bi-clock-history"></i>
            <h3>Activité Récente</h3>
        </div>
        
        <div class="activity-list">
            <!-- ACTIVITY 1 -->
            <div class="activity-item">
                <div class="activity-avatar">
                    <i class="bi bi-check-circle-fill"></i>
                </div>
                <div class="activity-content">
                    <div class="activity-title">Tâche terminée</div>
                    <div class="activity-description">
                        <strong>Baudouin</strong> a marqué "Intégration Dashboard Email" comme terminée
                    </div>
                    <div class="activity-time">
                        <i class="bi bi-clock"></i>
                        Il y a 10 minutes
                    </div>
                </div>
            </div>

            <!-- ACTIVITY 2 -->
            <div class="activity-item">
                <div class="activity-avatar">
                    <i class="bi bi-chat-dots-fill"></i>
                </div>
                <div class="activity-content">
                    <div class="activity-title">Nouveau message</div>
                    <div class="activity-description">
                        <strong>Assistant IA</strong> a envoyé une suggestion pour optimiser les workflows N8N
                    </div>
                    <div class="activity-time">
                        <i class="bi bi-clock"></i>
                        Il y a 2 heures
                    </div>
                </div>
            </div>

            <!-- ACTIVITY 3 -->
            <div class="activity-item">
                <div class="activity-avatar">
                    <i class="bi bi-person-plus-fill"></i>
                </div>
                <div class="activity-content">
                    <div class="activity-title">Nouveau membre</div>
                    <div class="activity-description">
                        <strong>Marie Dupont</strong> a rejoint l'équipe MIDDO en tant que Designer
                    </div>
                    <div class="activity-time">
                        <i class="bi bi-clock"></i>
                        Il y a 1 jour
                    </div>
                </div>
            </div>

            <!-- ACTIVITY 4 -->
            <div class="activity-item">
                <div class="activity-avatar">
                    <i class="bi bi-graph-up"></i>
                </div>
                <div class="activity-content">
                    <div class="activity-title">Mise à jour Analytics</div>
                    <div class="activity-description">
                        Synchronisation Google Sheets terminée : 4 nouveaux graphiques exportés
                    </div>
                    <div class="activity-time">
                        <i class="bi bi-clock"></i>
                        Il y a 1 jour
                    </div>
                </div>
            </div>

            <!-- ACTIVITY 5 -->
            <div class="activity-item">
                <div class="activity-avatar">
                    <i class="bi bi-envelope-fill"></i>
                </div>
                <div class="activity-content">
                    <div class="activity-title">Email Dashboard envoyé</div>
                    <div class="activity-description">
                        Rapport quotidien envoyé avec succès à <strong>mbane.baudouin@gmail.com</strong>
                    </div>
                    <div class="activity-time">
                        <i class="bi bi-clock"></i>
                        Il y a 1 jour
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

{% endblock %}
