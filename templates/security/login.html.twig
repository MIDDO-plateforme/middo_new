{# ============================================================
   FICHIER : templates/security/login.html.twig
   ============================================================ #}

<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Connexion – MIDDO</title>

    <link rel="stylesheet" href="/css/login.css">
</head>
<body>

<div class="login-page">

    <div class="login-card">
        <div class="login-logo">
            <h1>MIDDO</h1>
            <p>Assistant IA pour cliniques, ONG et entrepreneurs.</p>
        </div>

        {# 🔥 IMPORTANT : le formulaire doit pointer vers app_login #}
        <form class="login-form" method="post" action="{{ path('app_login') }}">

            {% if error %}
                <div class="login-error">
                    Identifiants incorrects.
                </div>
            {% endif %}

            <div class="form-group">
                <label for="email">Email</label>
                <input id="email"
                       name="_username"
                       type="email"
                       required
                       value="{{ last_username }}"
                       placeholder="vous@exemple.org">
            </div>

            <div class="form-group">
                <label for="password">Mot de passe</label>
                <input id="password"
                       name="_password"
                       type="password"
                       required
                       placeholder="••••••••">
            </div>

            <button type="submit" class="btn-login">Se connecter</button>

            <p class="login-hint">
                Accès sécurisé au cockpit MIDDO.
            </p>
        </form>
    </div>

</div>

</body>
</html>
