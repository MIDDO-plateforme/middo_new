<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}MIDDO{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    {# CSS principal MIDDO #}
    <link rel="stylesheet" href="{{ asset('assets/styles/middo.css') }}">

    {# Thème des formulaires #}
    {% if form is defined %}
        {% form_theme form 'form/form_layout.html.twig' %}
    {% endif %}
</head>

<body class="mdo-page">

<header class="mdo-header">
    <div style="display:flex;justify-content:space-between;align-items:center;">
        <div>
            <strong>MIDDO</strong> — Annuaire & IA
        </div>

        <nav style="display:flex;gap:0.5rem;">
            <a class="mdo-btn" href="{{ path('annuaire_index') }}">Annuaire</a>
            <a class="mdo-nav-link" href="{{ path('account_settings') }}">Mon compte</a>

            {# Centre d'achat #}
            <a class="mdo-nav-link" href="{{ path('product_list') }}">Centre d’Achat</a>
            <a class="mdo-nav-link" href="{{ path('order_my_orders') }}">Mes commandes</a>
            <a class="mdo-nav-link" href="{{ path('order_received') }}">Commandes reçues</a>
            <a class="mdo-nav-link" href="{{ path('purchase_ai') }}">Recommandations IA</a>
        </nav>
    </div>
</header>

<main class="mdo-main">
    {% block body %}{% endblock %}
</main>

<footer class="mdo-footer">
    MIDDO — Soulager les démarches, connecter les talents, protéger les plus vulnérables.
</footer>

</body>
</html>
